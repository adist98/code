#include <string>
#include <vector>
#include <iostream>
#include <bits/stdc++.h> 
using namespace std;

int main()
{
	string s;
	cin >> s;
	int cout = 0;
	int n = s.length();  
	
	char arr[n];  
      
    // copying the contents of the  
    // string to char array 
    strcpy(arr, s.c_str());  
      
    for (int i=0; i<n; i++){
    	if(arr[i]="a"){
    		if(arr[i+1]="a" ){
    			count = count + 1
			}
		}
    	
    	
    	
	} 
    cout << count       
  
    return 0; 	
	
	
}
